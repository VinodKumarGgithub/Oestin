const initState={
    hotels:[],
    isLoading:false,
    isError:false
}
export const reducer = (state, {type , payload}) => {

    switch (type) {
        case '':
            
            break;
    
        default:
            return state;
    }
}